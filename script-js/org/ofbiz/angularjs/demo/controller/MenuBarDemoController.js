package org.ofbiz.angularjs.demo.controller;

/**
 * Menu Bar Demo Controller
 * 
 * @param $scope
 * @param $window
 * @param $location
 */
function MenuBarDemoController($scope, $window, $location) {
    $scope.$location = $location;
}
